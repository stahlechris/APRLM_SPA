using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Content.cs adds Containers to a Content panel
/// </summary>
public class Content : MonoBehaviour
{
    public void AddContainer() //when they press enter, it makes a new container
    {
        //so i would enter my pose then hit enter
        //then the "press enter to..." would get deleted (todo figure out the exact coords from the layoutGroup to reuse this object)
        //then a new container would be added
        //then a "press enter to..." prefab would be instantiated
    }
}
